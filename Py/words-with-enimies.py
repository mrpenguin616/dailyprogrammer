"""
    words with enemies - daily programmer challenge

    author - mrpenguin616
    doenst work i fucking suck
"""


' places words in words list ' 
inputs = [line.rstrip('\n') for line in open('Inputs/words-with-enemies.txt')]
print inputs
#for groups in inputs:
#   battle = groups.strip()
#    print battle
   # for c in left + right:
   #    print 'something'

#for c in left + right:
#    if c in left and c in right:
#        left, right = left.replace(c, "", 1), right.replace(c, "", 1)
#
#    print left , ',' , right
#    print ("Tie" if len(left)==len(right) else ("Left wins" if len(left)>len(right) else "Right wins"))

"""
for l,r in zip(words[0::2], words[1::2]):
    print l.strip(r), r.strip(l)
"""
